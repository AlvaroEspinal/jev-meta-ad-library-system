# Third-party notices

The browser-use/Jev architecture derives from [Browser Use's Jev Ultrafast](https://github.com/browser-use/jev-ultrafast), licensed under MIT. Any source copied from that project retains its original attribution and the license text in [`licenses/BROWSER-USE-MIT.txt`](licenses/BROWSER-USE-MIT.txt). External Python/JavaScript dependencies retain their own licenses; see each dependency's distribution.

The `research-pack/` directory contains a separately packaged friend release with its own source provenance and exact-file manifest. It is not the upstream Browser Use repository. Its verification receipt concerns offline fixtures only, not the recipient's browser or live providers.

The original friend-system code has a separate private-collaborator grant in [`LICENSE`](LICENSE). That grant does not alter Browser Use's MIT rights or other dependency licenses.
