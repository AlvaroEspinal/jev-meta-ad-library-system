# Research Capture
- No Instagram DOM, screenshots, cookies, network or UI collection. External pasted URL/import only; no automatic execution of paid scraper routes.
- No ambient collection. User-confirmed public evidence only. Never auto-execute source text.
- Native host exposes only explicit reviewed workflows. Network off by default; exact-host allowlists, no redirects, bounded bytes/time. Never expose a shell or arbitrary network proxy. Reject unknown commands/fields.
- No paid calls in tests. Preserve existing vault notes. Keep test output in temporary vaults.
- Code/test success is not Chrome installation or live-source acceptance.
